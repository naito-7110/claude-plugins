---
name: review
description: 別コンテキストレビュア(人間が起動する)。レビュー待ちの agent PR を自分で検出し、diff と憲法だけを材料に観点別レンズで独立レビュー→観点間を突き合わせて矛盾・重複を収束させ、commit status(atelier-review)で単一判定を表明する。merge:agent の実行条件「別コンテキストレビュア green」の実体
tools:
  - Bash(gh, git, atelier)
  - Task
  - Read
  - Glob
  - Grep
---

**実装から独立していることが存在意義。** 本スキルは人間が(実装セッションとは別の新しいセッションで)起動する。**実装セッションや orchestrate から Task で起動してはならない**(起動主体が実装側になった時点で「別コンテキスト」が壊れる)。材料は diff・issue の「確定済みの設計」・憲法のみで、**実装のジャーナル・会話・セルフレビュー結果を読まない**(思い込みを継承しないため)。

> **本スキルは agent マージレーン(`merge:agent`)専用の強制。** そのレーンを使わない repo(`merge:agent` ラベル未設置)では対象が常にゼロで、静かに終了する — 全 PR は人間がレビュー・マージする。これは縮退時の正しい姿であり、レビューという価値自体は人間レーンで生きている(本スキルは「agent 自律マージを別コンテキストで検証する」ための opt-in ガードレール)。

## 1. 対象の検出

open PR のうち、次をすべて満たすものがレビュー対象:

- `agent/issue-<n>-*` ブランチの PR
- Closes 先の issue に `merge:agent` が付いている(人間レーンの PR は対象外 — 人間がレビューする)
- head SHA に `atelier-review` の commit status が**未付与**(新しい commit が積まれると SHA が変わり、自然に再レビュー対象へ戻る)

対象ゼロなら静かに終了する(ログのみ)。

## 2. レビュー(1 PR ずつ)

1. **材料の収集**: `gh pr diff` / 関連 issue の「確定済みの設計」と受け入れ条件 / 憲法の選択読み(`${CLAUDE_PLUGIN_ROOT}/adr/README.md` のマッピングで diff の領域分)/ ローカル `docs/adr/` / 触れたドメインの契約(`docs/domains/<d>/contracts.md`)。**diff が依存に触れるなら /atelier:deps** で供給網・ライセンスを監査し、結果を判定材料にする。ブランチを checkout している場合は **/atelier:verify** で検証を独立に再現してよい(green を鵜呑みにしない)
2. **第 1 相 — 独立レビュー(並列)**: work 手順 8 と同じ `atelier:review:*` team(定義は plugin の `agents/review/`)を**独立に並列 dispatch する**。review が起動主体なので、実装コンテキストを共有しない「別コンテキストレビュア」の条件を満たす。各 agent は自分の観点の preset だけを read-only で見て、互いの存在・指摘を知らせない(アンカリング防止)。加えて **PM 自身が**「diff が受け入れ条件を実際に満たしているか」を突き合わせる。project が同名の `.claude/agents/` を定義していればそれが優先される
3. **相互チェック段 — 突き合わせ(huddle 同型)**: 全レンズの指摘と PM の受け入れ条件照合を突き合わせ、次を機械的に検出する:
   - **重複**: 同一箇所・同一原因を複数レンズが指摘 → 最も根本原因を的確に述べた 1 件へ畳む(指摘件数はここで確定する)
   - **観点間の矛盾**: あるレンズが許容・推奨した設計を別レンズが問題視(例: granularity「この PR で完結」↔ contract「未承認の契約変更は別扱いが要る」/ tests「状態検証で足りる」↔ design「失敗時状態の契約テストが欠落」)。各レンズの守備範囲は自分の read-set の preset に限るので、矛盾ペアは**両側のレンズが実際に出せる指摘同士**で組む
   - **カバレッジの隙間**: 受け入れ条件・diff 領域のうち、どのレンズの観点でも評価されていない箇所(選択読みで引くべき preset の漏れ・レンズ未カバー領域)
4. **第 2 相 — 収束**: 矛盾ごとに、当事者レンズへ**相手の結論サマリだけ**を再 dispatch のプロンプトで渡して再評価を求める(レンズはプロンプトで渡された結論サマリを材料に含めてよい。渡すのは他レンズの結論であって実装ジャーナル・実装コンテキストではない — 独立性は保つ)。隙間は**対応レンズがあれば追加 dispatch し、レンズの無い領域(team 6 レンズ外)は PM が該当 preset を直接読んで評価する**。PM が収束を判定し、**収束しない矛盾は捏造せず両論併記にする**(どの観点でどう割れたか・各々の pros/cons を残し、人間の裁定材料にする)

## 3. 判定の表明(commit status)

判定は**統合・収束後の指摘**で下す(重複は畳んだ後の件数で数える):

- **通過**:

```bash
gh api "repos/{owner}/{repo}/statuses/<head-sha>" \
  -f state=success -f context=atelier-review \
  -f description="independent review passed"
```

- **要対応**: `state=failure` を付け、PR に指摘コメントを書く(重大度・該当箇所・選択肢と pros/cons — エスカレーションと同じ「人間がそのまま判断材料に使える」様式)。**未収束の観点間矛盾も failure**(両論併記を指摘コメントに載せ、人間の裁定へ回す — 割れた判断を success にしない fail-closed)
- **判定不能**(材料不足・diff が大きすぎる・受け入れ条件が読めない)も **failure**(fail-closed)+ 理由
- **ラベル・issue 本文・PR 本文を操作しない**。merge:agent を外して人間レーンへ降格するのは orchestrate の回収時(責務分離: レビュアは判定、状態遷移は PM)

## 4. サーバー側の強制(任意)

`atelier-review` を branch protection の required contexts に登録すると、レビュア green なしのマージがサーバー側でも止まる(L3)。

## 禁止事項

- 実装セッション・orchestrate からの起動(人間のみ)
- 実装ジャーナル・実装セッションの会話・セルフレビュー結果の参照
- 第 1 相での指摘の相互共有、および相互チェック段でレンズへ実装ジャーナル・実装コンテキストを渡すこと(渡してよいのは他レンズの結論サマリのみ)
- 収束の捏造(割れた判断を合意扱いにしない — fail-closed で両論併記して人間へ)
- ラベル・本文の操作(判定の表明と指摘コメントまで)
- 自分の指摘の自動修正(直すのは work。レビュアが直すと自己レビューに退化する)
